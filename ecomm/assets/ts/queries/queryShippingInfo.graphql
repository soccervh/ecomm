query QueryShippingInfo($id: Int) {
    shippingInfo(id: $id) {
        user {
            id
        }
        address
        city
        name
        state
        zip
        id
        lastUsed
    }
}
