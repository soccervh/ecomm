mutation SignUpMutation($input:UserMutationInput) {
    signUp(input:$input) {
        user {
            username
            id
            firstName
            lastName
            email
            password
        }
    }
}
