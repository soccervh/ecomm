query QueryProduct($slug: String!) {
    getProduct(slug: $slug) {
        category {
            id
            name
        }
        name
        price
        id
        description
        qtyInStock
        skuNumber
        slug
        productPic
    }
}
