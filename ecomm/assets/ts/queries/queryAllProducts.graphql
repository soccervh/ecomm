query QueryAllProducts($categorySlug: String) {
    allProducts (categorySlug: $categorySlug){
        name
        price
        id
        description
        qtyInStock
        skuNumber
        slug
        productPic
    }
}
